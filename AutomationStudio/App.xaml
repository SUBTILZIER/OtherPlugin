<Application x:Class="AutomationStudioWpf.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:AutomationStudioWpf"
             xmlns:po="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"
             ShutdownMode="OnMainWindowClose"
             >
    <Application.Resources>
        <!-- Runtime themes mutate these brushes. Keep them unfrozen; otherwise existing StaticResource users keep stale colors. -->
        <SolidColorBrush x:Key="AppBackgroundBrush" Color="#1B1D1F" po:Freeze="False" />
        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#222426" po:Freeze="False" />
        <SolidColorBrush x:Key="PanelAltBackgroundBrush" Color="#292C30" po:Freeze="False" />
        <SolidColorBrush x:Key="BorderBrushDark" Color="#3A4048" po:Freeze="False" />
        <SolidColorBrush x:Key="ForegroundPrimaryBrush" Color="#D8DCE3" po:Freeze="False" />
        <SolidColorBrush x:Key="ForegroundSecondaryBrush" Color="#9CA3AD" po:Freeze="False" />
        <SolidColorBrush x:Key="AccentBrush" Color="#3E9BB5" po:Freeze="False" />
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#65B3FF" po:Freeze="False" />
        <SolidColorBrush x:Key="AccentPressedBrush" Color="#357AD8" po:Freeze="False" />
        <SolidColorBrush x:Key="AccentForegroundBrush" Color="#FFFFFF" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorSelectionTextBrush" Color="#F2F5F8" po:Freeze="False" />
        <SolidColorBrush x:Key="InputBackgroundBrush" Color="#272B30" po:Freeze="False" />
        <SolidColorBrush x:Key="InputBorderBrush" Color="#454C55" po:Freeze="False" />
        <SolidColorBrush x:Key="DisabledInputBackgroundBrush" Color="#202328" po:Freeze="False" />
        <SolidColorBrush x:Key="DisabledInputBorderBrush" Color="#353A41" po:Freeze="False" />
        <SolidColorBrush x:Key="DisabledForegroundBrush" Color="#686F79" po:Freeze="False" />
        <SolidColorBrush x:Key="DropdownBackgroundBrush" Color="#272B30" po:Freeze="False" />
        <SolidColorBrush x:Key="DropdownHoverBrush" Color="#32383F" po:Freeze="False" />
        <SolidColorBrush x:Key="DropdownSelectedBrush" Color="#294A5B" po:Freeze="False" />
        <SolidColorBrush x:Key="DropdownBorderBrush" Color="#454C55" po:Freeze="False" />
        <SolidColorBrush x:Key="DropdownTextBrush" Color="#D8DCE3" po:Freeze="False" />
        <SolidColorBrush x:Key="DropdownMutedTextBrush" Color="#9CA3AD" po:Freeze="False" />
        <SolidColorBrush x:Key="ToolbarButtonBackgroundBrush" Color="#222426" po:Freeze="False" />
        <SolidColorBrush x:Key="ToolbarButtonBorderBrush" Color="#3A4048" po:Freeze="False" />
        <SolidColorBrush x:Key="CompileDirtyBackgroundBrush" Color="#4A3216" po:Freeze="False" />
        <SolidColorBrush x:Key="CompileDirtyBorderBrush" Color="#D88A3D" po:Freeze="False" />
        <SolidColorBrush x:Key="StatusMutedBrush" Color="#858C95" po:Freeze="False" />
        <SolidColorBrush x:Key="LogErrorBrush" Color="#E06C75" po:Freeze="False" />
        <SolidColorBrush x:Key="ValidationErrorBrush" Color="#E06C75" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorRootBackgroundBrush" Color="#1B1D1F" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorWindowBorderBrush" Color="#3A4048" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorPanelBackgroundBrush" Color="#222426" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorPanelBorderBrush" Color="#3A4048" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorPanelStrongBorderBrush" Color="#4A525C" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorCanvasBackgroundBrush" Color="#17191C" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorCanvasGridBackgroundBrush" Color="#1B1E22" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorCanvasGridLineBrush" Color="#292E34" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorNodeBackgroundBrush" Color="#E624272B" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorExecutionOverlayBrush" Color="#B0181D2A" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorExecutionPinBrush" Color="#F4F4F4" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorCompletionPinBrush" Color="#FFB848" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorFailurePinBrush" Color="#FF5A64" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorBooleanPinBrush" Color="#B82D30" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorVectorPinBrush" Color="#50C472" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorStringPinBrush" Color="#CA2EA5" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorDefaultPinBrush" Color="#B8BEC7" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorConnectionOutlineBrush" Color="#CC101214" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorConnectionHitBrush" Color="#2F9FD0" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorPreviewConnectionBrush" Color="#E1E7EA" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorSectionHeaderBrush" Color="#292C30" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorListSelectedBrush" Color="#294A5B" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorListHoverBrush" Color="#32383F" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorDirtyBackgroundBrush" Color="#3F2A13" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorTextBrush" Color="#D8DCE3" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorTextBrightBrush" Color="#E7EAEE" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorMutedTextBrush" Color="#9CA3AD" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorPanelCardBrush" Color="#292C30" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorPanelCardHoverBrush" Color="#32383F" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorPanelElevatedBrush" Color="#25282C" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorChromeBrush" Color="#1B1E22" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorChromeHighlightBrush" Color="#32383F" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorToolbarGroupBrush" Color="#222426" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorToolbarSeparatorBrush" Color="#3A4048" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorSectionHeaderAccentBrush" Color="#263C45" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorSelectedAccentBrush" Color="#3E9BB5" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorSelectedBorderBrush" Color="#3E9BB5" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorFieldCardBrush" Color="#272B30" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorDisabledChipBrush" Color="#202328" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorDisabledChipBorderBrush" Color="#353A41" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorToolTipBackgroundBrush" Color="#14171A" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorToolTipBorderBrush" Color="#3E9BB5" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorToolTipTextBrush" Color="#F2F4F7" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorSelectionGlowBrush" Color="#3E9BB5" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorSelectionRingBrush" Color="#101214" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorSelectionBorderBrush" Color="#3E9BB5" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorSelectionFillBrush" Color="#335C97A8" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorNodeNumberBackgroundBrush" Color="#66000000" po:Freeze="False" />
        <Color x:Key="EditorSelectionGlowColor">#FF3E9BB5</Color>

        <Style x:Key="EditorVerticalSplitterStyle" TargetType="{x:Type GridSplitter}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="VerticalAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type GridSplitter}">
                        <Grid Background="{TemplateBinding Background}">
                            <Border x:Name="SplitterIndicator"
                                    Width="1"
                                    HorizontalAlignment="Center"
                                    Background="{DynamicResource EditorPanelBorderBrush}" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="SplitterIndicator" Property="Width" Value="3" />
                                <Setter TargetName="SplitterIndicator" Property="Background" Value="{DynamicResource EditorSelectedAccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="SplitterIndicator" Property="Width" Value="3" />
                                <Setter TargetName="SplitterIndicator" Property="Background" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="EditorHorizontalSplitterStyle" TargetType="{x:Type GridSplitter}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="VerticalAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type GridSplitter}">
                        <Grid Background="{TemplateBinding Background}">
                            <Border x:Name="SplitterIndicator"
                                    Height="1"
                                    VerticalAlignment="Center"
                                    Background="{DynamicResource EditorPanelBorderBrush}" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="SplitterIndicator" Property="Height" Value="3" />
                                <Setter TargetName="SplitterIndicator" Property="Background" Value="{DynamicResource EditorSelectedAccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="SplitterIndicator" Property="Height" Value="3" />
                                <Setter TargetName="SplitterIndicator" Property="Background" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkContextMenuStyle" TargetType="ContextMenu">
            <Setter Property="Background" Value="{DynamicResource DropdownBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource DropdownBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{DynamicResource DropdownTextBrush}" />
            <Setter Property="Padding" Value="4" />
            <Setter Property="HasDropShadow" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ContextMenu}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <StackPanel IsItemsHost="True"
                                        KeyboardNavigation.DirectionalNavigation="Cycle" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="MenuItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource DropdownTextBrush}" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type MenuItem}">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter ContentSource="Header"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource DropdownHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource DisabledForegroundBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ToolTip}">
            <Setter Property="Foreground" Value="{DynamicResource EditorToolTipTextBrush}" />
            <Setter Property="Background" Value="{DynamicResource EditorToolTipBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource EditorToolTipBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="MaxWidth" Value="360" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToolTip}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="18"
                                                  ShadowDepth="4"
                                                  Opacity="0.42"
                                                  Color="#000000" />
                            </Border.Effect>
                            <ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}"
                                              TextElement.FontSize="{TemplateBinding FontSize}"
                                              RecognizesAccessKey="True" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Window">
            <Setter Property="Icon" Value="{x:Static local:WindowIconHelper.AppIcon}" />
            <Setter Property="Background" Value="{DynamicResource AppBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource ForegroundPrimaryBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI Variable, Microsoft YaHei UI, Segoe UI" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="UseLayoutRounding" Value="True" />
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundPrimaryBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI Variable, Microsoft YaHei UI, Segoe UI" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundPrimaryBrush}" />
            <Setter Property="Background" Value="{DynamicResource PanelAltBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushDark}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="MinHeight" Value="30" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="ToolTipService.ShowOnDisabled" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource DropdownHoverBrush}" />
                                <Setter Property="BorderBrush" Value="{DynamicResource AccentHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource DropdownSelectedBrush}" />
                                <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                <Setter TargetName="ButtonBorder" Property="BorderThickness" Value="2" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundPrimaryBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="Padding" Value="2,1" />
            <Setter Property="MinHeight" Value="24" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="ToolTipService.ShowOnDisabled" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <Border x:Name="CheckBoxChrome"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <DockPanel LastChildFill="True">
                                <Border x:Name="Box"
                                        Width="14"
                                        Height="14"
                                        Margin="0,0,7,0"
                                        VerticalAlignment="Center"
                                        Background="{DynamicResource EditorFieldCardBrush}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="3">
                                    <Path x:Name="CheckMark"
                                          Data="M 2,7 L 5.5,10.5 L 12,3"
                                          Stroke="{DynamicResource AccentForegroundBrush}"
                                          StrokeThickness="1.7"
                                          StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round"
                                          Visibility="Collapsed" />
                                </Border>
                                <ContentPresenter VerticalAlignment="Center"
                                                  RecognizesAccessKey="True"
                                                  TextElement.Foreground="{TemplateBinding Foreground}" />
                            </DockPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource AccentHoverBrush}" />
                                <Setter TargetName="CheckBoxChrome" Property="Background" Value="{DynamicResource DropdownHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Box" Property="Background" Value="{DynamicResource AccentBrush}" />
                                <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                <Setter TargetName="Box" Property="BorderThickness" Value="2" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundPrimaryBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="{DynamicResource StatusMutedBrush}" />
            <Setter Property="Padding" Value="4,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="ToolTipService.ShowOnDisabled" Value="True" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RadioButton}">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <DockPanel LastChildFill="True">
                                <Grid Width="12"
                                      Height="12"
                                      Margin="0,0,5,0"
                                      VerticalAlignment="Center">
                                    <Ellipse x:Name="OuterRing"
                                             Stroke="{TemplateBinding BorderBrush}"
                                             StrokeThickness="1.2"
                                             Fill="{DynamicResource DropdownBackgroundBrush}" />
                                    <Ellipse x:Name="CheckedDot"
                                             Width="6"
                                             Height="6"
                                             Fill="{DynamicResource ForegroundPrimaryBrush}"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center"
                                             Visibility="Collapsed" />
                                </Grid>
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  RecognizesAccessKey="True"
                                                  TextElement.Foreground="{TemplateBinding Foreground}" />
                            </DockPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="OuterRing" Property="Stroke" Value="{DynamicResource AccentHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="OuterRing" Property="Stroke" Value="{DynamicResource ForegroundPrimaryBrush}" />
                                <Setter TargetName="CheckedDot" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="OuterRing" Property="Stroke" Value="{DynamicResource AccentBrush}" />
                                <Setter TargetName="OuterRing" Property="StrokeThickness" Value="2" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundPrimaryBrush}" />
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CaretBrush" Value="{DynamicResource ForegroundPrimaryBrush}" />
            <Setter Property="MinHeight" Value="30" />
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="TextBoxBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                          HorizontalScrollBarVisibility="{TemplateBinding HorizontalScrollBarVisibility}"
                                          VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}"
                                          Focusable="False" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="AcceptsReturn" Value="True">
                    <Setter Property="VerticalContentAlignment" Value="Top" />
                </Trigger>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="BorderThickness" Value="2" />
                    <Setter Property="Background" Value="{DynamicResource DropdownHoverBrush}" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{DynamicResource DisabledInputBackgroundBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource DisabledInputBorderBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource DisabledForegroundBrush}" />
                    <Setter Property="Opacity" Value="1" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="{DynamicResource DropdownTextBrush}" />
            <Setter Property="Background" Value="{DynamicResource DropdownBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource DropdownBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="MinHeight" Value="30" />
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="20" />
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1"
                                                      Data="M 0 0 L 4 4 L 8 0 Z"
                                                      Fill="{DynamicResource DropdownMutedTextBrush}"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      StrokeThickness="0" />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource DropdownHoverBrush}" />
                                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource AccentHoverBrush}" />
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource DropdownSelectedBrush}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="8,4,24,4"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              IsHitTestVisible="False"
                                              TextBlock.Foreground="{TemplateBinding Foreground}" />
                            <TextBox x:Name="PART_EditableTextBox"
                                      Visibility="Hidden"
                                      Background="{DynamicResource DropdownBackgroundBrush}"
                                      Foreground="{DynamicResource DropdownTextBrush}"
                                      BorderThickness="0"
                                      CaretBrush="{DynamicResource DropdownTextBrush}"
                                      Margin="8,4,24,4"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Left"
                                      IsReadOnly="{TemplateBinding IsReadOnly}" />
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                 <Border x:Name="DropDownBorder"
                                         Background="{DynamicResource DropdownBackgroundBrush}"
                                         BorderBrush="{DynamicResource DropdownBorderBrush}"
                                         BorderThickness="1"
                                         CornerRadius="4"
                                         Margin="0,2,0,0"
                                         MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                         MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer Background="{DynamicResource DropdownBackgroundBrush}">
                                         <ItemsPresenter />
                                     </ScrollViewer>
                                 </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden" />
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{DynamicResource DisabledInputBackgroundBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource DisabledInputBorderBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource DisabledForegroundBrush}" />
                    <Setter Property="Opacity" Value="1" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource DropdownTextBrush}" />
            <Setter Property="Background" Value="{DynamicResource DropdownBackgroundBrush}" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="2">
                            <ContentPresenter TextBlock.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource DropdownHoverBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource DropdownTextBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource DropdownSelectedBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource EditorSelectionTextBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource DisabledForegroundBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource EditorPanelStrongBorderBrush}" />
            <Setter Property="Opacity" Value="0.72" />
            <Setter Property="ToolTipService.ShowOnDisabled" Value="True" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="1" />
                    <Setter Property="Foreground" Value="{DynamicResource EditorSelectedBorderBrush}" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.35" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DarkDropdownListBoxItemStyle" TargetType="{x:Type ListBoxItem}">
            <Setter Property="Foreground" Value="{DynamicResource DropdownTextBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="4,2" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              TextBlock.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource DropdownHoverBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource DropdownTextBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource DropdownSelectedBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource EditorSelectionTextBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource DisabledForegroundBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkDropdownListBoxStyle" TargetType="{x:Type ListBox}">
            <Setter Property="Foreground" Value="{DynamicResource DropdownTextBrush}" />
            <Setter Property="Background" Value="{DynamicResource DropdownBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource DropdownBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="ItemContainerStyle" Value="{StaticResource DarkDropdownListBoxItemStyle}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBox}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">
                            <ScrollViewer Background="{TemplateBinding Background}"
                                          Focusable="False">
                                <ItemsPresenter />
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ToolBar">
            <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource ForegroundPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushDark}" />
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Margin" Value="8,0" />
        </Style>
    </Application.Resources>
</Application>
