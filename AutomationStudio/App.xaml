<Application x:Class="AutomationStudioWpf.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:AutomationStudioWpf"
             xmlns:po="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"
             ShutdownMode="OnMainWindowClose"
             >
    <Application.Resources>
        <!-- Runtime themes mutate these brushes. Keep them unfrozen; otherwise existing StaticResource users keep stale colors. -->
        <SolidColorBrush x:Key="AppBackgroundBrush" Color="#171C24" po:Freeze="False" />
        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#1C232D" po:Freeze="False" />
        <SolidColorBrush x:Key="PanelAltBackgroundBrush" Color="#222A35" po:Freeze="False" />
        <SolidColorBrush x:Key="BorderBrushDark" Color="#303A48" po:Freeze="False" />
        <SolidColorBrush x:Key="ForegroundPrimaryBrush" Color="#E6EDF5" po:Freeze="False" />
        <SolidColorBrush x:Key="ForegroundSecondaryBrush" Color="#AAB6C6" po:Freeze="False" />
        <SolidColorBrush x:Key="AccentBrush" Color="#4FA3FF" po:Freeze="False" />
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#65B3FF" po:Freeze="False" />
        <SolidColorBrush x:Key="AccentPressedBrush" Color="#357AD8" po:Freeze="False" />
        <SolidColorBrush x:Key="AccentForegroundBrush" Color="#FFFFFF" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorSelectionTextBrush" Color="#F3F7FF" po:Freeze="False" />
        <SolidColorBrush x:Key="InputBackgroundBrush" Color="#232B36" po:Freeze="False" />
        <SolidColorBrush x:Key="InputBorderBrush" Color="#3C4A5C" po:Freeze="False" />
        <SolidColorBrush x:Key="DisabledInputBackgroundBrush" Color="#1A2028" po:Freeze="False" />
        <SolidColorBrush x:Key="DisabledInputBorderBrush" Color="#303A48" po:Freeze="False" />
        <SolidColorBrush x:Key="DisabledForegroundBrush" Color="#7C8796" po:Freeze="False" />
        <SolidColorBrush x:Key="DropdownBackgroundBrush" Color="#151B23" po:Freeze="False" />
        <SolidColorBrush x:Key="DropdownHoverBrush" Color="#203044" po:Freeze="False" />
        <SolidColorBrush x:Key="DropdownSelectedBrush" Color="#2D6FB7" po:Freeze="False" />
        <SolidColorBrush x:Key="DropdownBorderBrush" Color="#344154" po:Freeze="False" />
        <SolidColorBrush x:Key="DropdownTextBrush" Color="#EAF1FA" po:Freeze="False" />
        <SolidColorBrush x:Key="DropdownMutedTextBrush" Color="#AAB6C6" po:Freeze="False" />
        <SolidColorBrush x:Key="ToolbarButtonBackgroundBrush" Color="#171E28" po:Freeze="False" />
        <SolidColorBrush x:Key="ToolbarButtonBorderBrush" Color="#303A48" po:Freeze="False" />
        <SolidColorBrush x:Key="CompileDirtyBackgroundBrush" Color="#4A3216" po:Freeze="False" />
        <SolidColorBrush x:Key="CompileDirtyBorderBrush" Color="#D68A22" po:Freeze="False" />
        <SolidColorBrush x:Key="StatusMutedBrush" Color="#8793A4" po:Freeze="False" />
        <SolidColorBrush x:Key="LogErrorBrush" Color="#FF6B6B" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorRootBackgroundBrush" Color="#10151D" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorWindowBorderBrush" Color="#303A48" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorPanelBackgroundBrush" Color="#171C24" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorPanelBorderBrush" Color="#293241" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorPanelStrongBorderBrush" Color="#354255" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorCanvasBackgroundBrush" Color="#0D1219" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorCanvasGridBackgroundBrush" Color="#111821" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorCanvasGridLineBrush" Color="#202936" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorNodeBackgroundBrush" Color="#20242A" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorExecutionOverlayBrush" Color="#B0181D2A" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorExecutionPinBrush" Color="#F4F4F4" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorConnectionHitBrush" Color="#2BA8FF" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorPreviewConnectionBrush" Color="#F5F5F5" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorSectionHeaderBrush" Color="#25303D" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorListSelectedBrush" Color="#2D6FB7" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorListHoverBrush" Color="#203044" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorDirtyBackgroundBrush" Color="#3F2A13" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorTextBrush" Color="#D7E0EC" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorTextBrightBrush" Color="#F3F7FF" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorMutedTextBrush" Color="#9EABBC" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorPanelCardBrush" Color="#151B23" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorPanelCardHoverBrush" Color="#1C2633" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorPanelElevatedBrush" Color="#1B2330" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorChromeBrush" Color="#111821" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorChromeHighlightBrush" Color="#203044" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorToolbarGroupBrush" Color="#151B24" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorToolbarSeparatorBrush" Color="#334155" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorSectionHeaderAccentBrush" Color="#1E2C3D" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorSelectedAccentBrush" Color="#4FA3FF" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorSelectedBorderBrush" Color="#2E7BC5" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorFieldCardBrush" Color="#121922" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorDisabledChipBrush" Color="#202833" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorDisabledChipBorderBrush" Color="#364354" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorToolTipBackgroundBrush" Color="#0B1220" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorToolTipBorderBrush" Color="#4FA3FF" po:Freeze="False" />
        <SolidColorBrush x:Key="EditorToolTipTextBrush" Color="#F8FAFC" po:Freeze="False" />

        <Style x:Key="DarkContextMenuStyle" TargetType="ContextMenu">
            <Setter Property="Background" Value="{DynamicResource DropdownBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource DropdownBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{DynamicResource DropdownTextBrush}" />
            <Setter Property="Padding" Value="4" />
            <Setter Property="HasDropShadow" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ContextMenu}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <StackPanel IsItemsHost="True"
                                        KeyboardNavigation.DirectionalNavigation="Cycle" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="MenuItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource DropdownTextBrush}" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type MenuItem}">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter ContentSource="Header"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource DropdownHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource DisabledForegroundBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ToolTip}">
            <Setter Property="Foreground" Value="{DynamicResource EditorToolTipTextBrush}" />
            <Setter Property="Background" Value="{DynamicResource EditorToolTipBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource EditorToolTipBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="MaxWidth" Value="360" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToolTip}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="18"
                                                  ShadowDepth="4"
                                                  Opacity="0.42"
                                                  Color="#000000" />
                            </Border.Effect>
                            <ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}"
                                              TextElement.FontSize="{TemplateBinding FontSize}"
                                              RecognizesAccessKey="True" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Window">
            <Setter Property="Icon" Value="{x:Static local:WindowIconHelper.AppIcon}" />
            <Setter Property="Background" Value="{DynamicResource AppBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource ForegroundPrimaryBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI Variable, Microsoft YaHei UI, Segoe UI" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="UseLayoutRounding" Value="True" />
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundPrimaryBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI Variable, Microsoft YaHei UI, Segoe UI" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundPrimaryBrush}" />
            <Setter Property="Background" Value="{DynamicResource PanelAltBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushDark}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource AccentBrush}" />
                                <Setter Property="BorderBrush" Value="{DynamicResource AccentHoverBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource AccentForegroundBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource AccentPressedBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource AccentForegroundBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundPrimaryBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="{DynamicResource StatusMutedBrush}" />
            <Setter Property="Padding" Value="4,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RadioButton}">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <DockPanel LastChildFill="True">
                                <Grid Width="12"
                                      Height="12"
                                      Margin="0,0,5,0"
                                      VerticalAlignment="Center">
                                    <Ellipse x:Name="OuterRing"
                                             Stroke="{TemplateBinding BorderBrush}"
                                             StrokeThickness="1.2"
                                             Fill="{DynamicResource DropdownBackgroundBrush}" />
                                    <Ellipse x:Name="CheckedDot"
                                             Width="6"
                                             Height="6"
                                             Fill="{DynamicResource ForegroundPrimaryBrush}"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center"
                                             Visibility="Collapsed" />
                                </Grid>
                                <ContentPresenter HorizontalAlignment="Left"
                                                  VerticalAlignment="Center"
                                                  RecognizesAccessKey="True"
                                                  TextElement.Foreground="{TemplateBinding Foreground}" />
                            </DockPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="OuterRing" Property="Stroke" Value="{DynamicResource AccentHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="OuterRing" Property="Stroke" Value="{DynamicResource ForegroundPrimaryBrush}" />
                                <Setter TargetName="CheckedDot" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundPrimaryBrush}" />
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CaretBrush" Value="{DynamicResource ForegroundPrimaryBrush}" />
            <Setter Property="Padding" Value="8,5" />
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{DynamicResource DisabledInputBackgroundBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource DisabledInputBorderBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource DisabledForegroundBrush}" />
                    <Setter Property="Opacity" Value="1" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="{DynamicResource DropdownTextBrush}" />
            <Setter Property="Background" Value="{DynamicResource DropdownBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource DropdownBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="20" />
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1"
                                                      Data="M 0 0 L 4 4 L 8 0 Z"
                                                      Fill="{DynamicResource DropdownMutedTextBrush}"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      StrokeThickness="0" />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource DropdownHoverBrush}" />
                                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource AccentHoverBrush}" />
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource DropdownSelectedBrush}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="8,4,24,4"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              IsHitTestVisible="False"
                                              TextBlock.Foreground="{TemplateBinding Foreground}" />
                            <TextBox x:Name="PART_EditableTextBox"
                                      Visibility="Hidden"
                                      Background="{DynamicResource DropdownBackgroundBrush}"
                                      Foreground="{DynamicResource DropdownTextBrush}"
                                      BorderThickness="0"
                                      CaretBrush="{DynamicResource DropdownTextBrush}"
                                      Margin="8,4,24,4"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Left"
                                      IsReadOnly="{TemplateBinding IsReadOnly}" />
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                 <Border x:Name="DropDownBorder"
                                         Background="{DynamicResource DropdownBackgroundBrush}"
                                         BorderBrush="{DynamicResource DropdownBorderBrush}"
                                         BorderThickness="1"
                                         CornerRadius="4"
                                         Margin="0,2,0,0"
                                         MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                         MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer Background="{DynamicResource DropdownBackgroundBrush}">
                                         <ItemsPresenter />
                                     </ScrollViewer>
                                 </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden" />
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{DynamicResource DisabledInputBackgroundBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource DisabledInputBorderBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource DisabledForegroundBrush}" />
                    <Setter Property="Opacity" Value="1" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource DropdownTextBrush}" />
            <Setter Property="Background" Value="{DynamicResource DropdownBackgroundBrush}" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="2">
                            <ContentPresenter TextBlock.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource DropdownHoverBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource DropdownTextBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource DropdownSelectedBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource EditorSelectionTextBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource DisabledForegroundBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkDropdownListBoxItemStyle" TargetType="{x:Type ListBoxItem}">
            <Setter Property="Foreground" Value="{DynamicResource DropdownTextBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="4,2" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              TextBlock.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource DropdownHoverBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource DropdownTextBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource DropdownSelectedBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource EditorSelectionTextBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource DisabledForegroundBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkDropdownListBoxStyle" TargetType="{x:Type ListBox}">
            <Setter Property="Foreground" Value="{DynamicResource DropdownTextBrush}" />
            <Setter Property="Background" Value="{DynamicResource DropdownBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource DropdownBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="ItemContainerStyle" Value="{StaticResource DarkDropdownListBoxItemStyle}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBox}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">
                            <ScrollViewer Background="{TemplateBinding Background}"
                                          Focusable="False">
                                <ItemsPresenter />
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ToolBar">
            <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource ForegroundPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushDark}" />
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Margin" Value="8,0" />
        </Style>
    </Application.Resources>
</Application>
