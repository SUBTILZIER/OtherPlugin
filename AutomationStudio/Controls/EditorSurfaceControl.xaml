<UserControl x:Class="AutomationStudioWpf.Controls.EditorSurfaceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="1280"
             Background="#181B20">
    <Grid x:Name="SurfaceRoot"
          Background="#181B20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="GraphSidebarColumn" Width="240" MinWidth="180" MaxWidth="420" />
            <ColumnDefinition x:Name="GraphSidebarSplitterColumn" Width="Auto" />
            <ColumnDefinition x:Name="GraphCanvasColumn" Width="*" MinWidth="360" />
            <ColumnDefinition x:Name="InspectorSplitterColumn" Width="Auto" />
            <ColumnDefinition x:Name="InspectorColumn" Width="460" MinWidth="420" MaxWidth="720" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Margin="10"
                Background="#20242B"
                BorderBrush="#303744"
                BorderThickness="1"
                CornerRadius="6">
            <ContentControl x:Name="GraphSidebarHost" />
        </Border>

        <GridSplitter Grid.Column="1"
                      Width="5"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      Background="#303744"
                      ShowsPreview="True" />

        <Border Grid.Column="2"
                Margin="0,10"
                Background="#181B20"
                BorderBrush="#303744"
                BorderThickness="1"
                CornerRadius="6">
            <ContentControl x:Name="GraphCanvasHost" />
        </Border>

        <GridSplitter Grid.Column="3"
                      Width="5"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      Background="#303744"
                      ShowsPreview="True" />

        <Border Grid.Column="4"
                Margin="10"
                Background="#20242B"
                BorderBrush="#303744"
                BorderThickness="1"
                CornerRadius="6">
            <ContentControl x:Name="InspectorHost" />
        </Border>
    </Grid>
</UserControl>
